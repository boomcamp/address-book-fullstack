version: "3"
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: perndb
    ports:
      - 5432:5432
    volumes:
      - addressBook:/var/lib/postgresql/data
volumes:
  addressBook:
