version: "3"
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: fullstackDB
    ports:
      - 5432:5432
    volumes:
      - fullstackDB:/var/lib/postgresql/data
volumes:
  fullstackDB:
